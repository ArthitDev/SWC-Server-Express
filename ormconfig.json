{
  "type": "mysql", 
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "p@ssw0rd",
  "database": "swc_db",
  "synchronize": false,
  "logging": true,
  "entities": ["src/entities/**/*.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "cli": {
    "migrationsDir": "src/migration"
  }
}
